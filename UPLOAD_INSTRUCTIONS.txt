BeerFactory r40.4 · Profile birth date iOS fix

BASE HEAD:
36eb884a5b97737930b27e16de5d0e52f2f43620

TARGET BRANCH:
r40.4-react

REPLACE ONLY:
- src/pages/ProfilePage.tsx

Changes:
- visible date-field frame is now owned by a 100% width wrapper, so iOS Safari cannot draw it past the Profile card edge;
- wrapper clips any native WebKit intrinsic overflow;
- visible control height reduced to 40px;
- native input remains type=date and keeps existing save/backend behavior.

No backend, Supabase, Shift, Handover, Recipes, auth, roles, or user-data changes.

After upload verify exact HEAD, manifest/blob SHA, Typecheck, Build, Vercel preview, then Profile at 390px/iPhone.
