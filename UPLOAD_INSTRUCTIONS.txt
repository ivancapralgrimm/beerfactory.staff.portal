BFStaff r40.4 · Admin Recipe Editor v1

BASE HEAD
20e2f1a103ed05a0055cdc744fae9756ee0c1182

UPLOAD TO GITHUB BRANCH r40.4-react ONLY

REPLACE
src/features/admin/AdminPage.tsx

ADD
src/features/admin/AdminRecipesPanel.tsx
src/features/admin/recipe-admin-api.ts
R40_4_ADMIN_RECIPE_EDITOR.md

DO NOT upload cloudflare/worker-recipe-editor-v3.js as if it automatically deploys the Worker.
It is a reviewed deployment candidate kept in the package for the separate Cloudflare step.

AFTER FRONTEND UPLOAD
1. Read new HEAD.
2. Verify file/blob hashes against MANIFEST.json.
3. GitHub Actions Typecheck + Build must pass.
4. Verify exact-head Vercel status.
5. Login as existing admin.
6. Open Profile -> Управление персоналом -> Рецепты.
7. Visual QA only: do not press final Create/Save on real NocoDB during ordinary QA.
8. Verify form:
   - Бар/Кухня source selector;
   - status defaults to Актуальный;
   - BAR requires Состав;
   - KITCHEN requires Описание;
   - photo text says JPEG or PNG · до 1 МБ;
   - >1 MiB and non-JPEG/PNG are rejected client-side;
   - editing locks source.

WORKER STEP (SEPARATE)
Compare cloudflare/worker-recipe-editor-v3.js with the CURRENT live Worker source before deployment.
Prefer a separate r40.4 preview Worker and Vercel Preview env override.
Do not replace stable production Worker until release gate.
