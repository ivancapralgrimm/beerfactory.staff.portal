# BeerFactory Staff Portal · r40.3 QA

Baseline: r40.2
Base commit: 5cefe7e6bd049ccee94a79ad1f4b37b6fa1a9afe
Branch: r40.3-qa

## Scope

r40.3 is a QA / hardening release. No new product features.

## Locked decisions

- Do not restore the temporary recipe admin controls in recipe cards.
- A full recipe creator/editor will be designed later as a dedicated admin feature.
- NocoDB remains the data store; routine recipe management should move into Staff Portal through the protected Worker API.
- main remains the production baseline until r40.3 QA is complete.

## Verified at QA start

- r40.3-qa was created from the same commit as main.
- main and r40.3-qa were identical at QA start.
- Live GitHub Pages portal responds.
- Live Worker /menu responds with ok:true.
- Worker reports governance:true.
- Worker reports recipe_admin_write:true.
- Worker reports source_aware_ids:true.
- Frontend removes Draft recipes again in staffVisible().
- Service worker uses versioned shell/runtime caches and cleans older bf-* caches.
- Offline navigation falls back to cached index.html.
- Existing Supabase session can be read locally; failed profile fetch is caught, so an authenticated installation can still render offline.

## PWA fix in this package

- Adds 192x192 PNG icon.
- Adds 512x512 PNG icon.
- Adds 512x512 maskable icon.
- Adds 180x180 Apple touch icon.
- Adds icon declarations to manifest.json.
- Adds Apple touch icon and browser icon links to index.html.
- Adds all icon assets to the service worker shell cache.
- Bumps service-worker registration and cache names to r40-3-pwa1.

## Remaining QA after upload

1. 390px mobile authenticated pass.
2. Real PWA install/update/offline cycle.
3. Staff visibility with an actual Draft recipe.
4. Repeat the five r40.2 recipe-navigation scenarios.
