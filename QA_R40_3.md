# BeerFactory Staff Portal · r40.3 QA

Baseline: r40.2
Base commit: 5cefe7e6bd049ccee94a79ad1f4b37b6fa1a9afe
Branch: r40.3-qa
PR: #1 (Draft)

## Scope

r40.3 is a QA / hardening release. No new product features.

## Locked decisions

- Do not restore temporary recipe-admin controls inside recipe cards.
- A full recipe creator/editor will be designed later as a dedicated admin feature.
- NocoDB remains the data store; routine recipe management should move into Staff Portal through the protected Worker API.
- main remains the production baseline until r40.3 QA is complete.

## Verified

- r40.3-qa was created from the same r40.2 baseline as main.
- Draft PR #1 targets main from r40.3-qa and is mergeable without conflicts.
- Live GitHub Pages production portal responds.
- Live Worker /menu responds with ok:true and source:nocodb.
- Worker reports governance:true, recipe_admin_write:true and source_aware_ids:true.
- Live Worker response contains 444 recipe rows: 430 current, 14 archive, 0 draft.
- Frontend also filters Draft rows with staffVisible(), providing a second visibility guard.
- PWA icon files in GitHub match the prepared files byte-for-byte by Git blob SHA.
- Manifest declares 192x192, 512x512 and maskable 512x512 PNG icons.
- index.html declares Apple touch icon and 192x192 browser icon.
- Service worker cache version is r40-3-pwa1 and old bf-* caches are removed on activation.
- Offline navigation falls back to cached index.html.
- Current Knowledge uses training-data.txt and current Attestation uses question-banks.json; both are pre-cached.
- Shift workflow does not fake successful offline writes: failed RPCs restore UI/error state.
- Handover does not fake successful offline writes: failed Supabase writes show an error.
- 390px static responsive audit found no unguarded fixed width wider than the usable mobile viewport.
- Core responsive breakpoints exist for auth, dashboard, recipes, knowledge, handover, shift workflow, admin console and dialogs.

## Hotfix 1

Removed assets/icons/icon-source-user.png from service-worker CORE precache.
The 1254x1254 source file is retained in the repository for future icon exports but is not required at runtime.

## Remaining real-device QA

1. Install/update PWA from an HTTPS deployment of r40.3.
2. Launch an already-installed PWA with network disabled.
3. Restore network and verify connectivity/update UI.
4. Authenticated visual pass at 390px on a real/mobile-emulated browser.
5. End-to-end Draft test using a known Draft row in NocoDB.
6. Repeat the five r40.2 recipe-navigation scenarios after deployment.

Do not mark PR Ready for review until the real-device/deployed checks above pass.
